<a href="/">Home</a> || <a href="/pets">Pet Index</a> || <a href="/reviews">Reviews Index</a> || <a href="/favorites">Favorites</a>
</br></br> ||
<a href="javascript:history.back()">Go Back</a> ||

<h1>All Shelters</h1>

<%= link_to "New Shelter", '/shelters/new' %>

<% @shelters.each do |shelter| %>
  <h3><a href="/shelters/<%= shelter.id %>"><%= shelter.name %></a></h3>
  <p><%= shelter.address %></p>
  <p><%= shelter.city %></p>
  <p><%= shelter.state %></p>
  <p><%= shelter.zip %></p>
<% end %>
