<h1><%= @shelter.name %> Pets</h1>

<% @pets.each do |pet| %>
  <h3><%= pet.image %></h3>
  <h4><%= pet.name %></h4>
  <h4><%= pet.approximate_age %></h4>
  <h4><%= pet.sex %></h4>
<% end %>

<%= link_to "Create Pet", "/shelters/#{@shelter.id}/pets/new" %>
