<!DOCTYPE html>
<html>
  <head>
    <title>Adopt Don't Shop Paired</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
  </head>

  <body>
    <% flash.each do |type, message| %>
        <section class=<%= type %>>
          <p><%= message %></p>
        </section>
    <% end %>

    <p>|| <a href="javascript:history.back()">Back</a> || <a href="/">Home</a> || <a href="/shelters">Shelter Index</a> || <a href="/pets">Pet Index</a> ||<a href="/reviews">Reviews Index</a> || <a href="/favorites">Favorites:</a> <%= @favorites.total_count %> </p>
  <%= yield %>
  </body>
</html>
